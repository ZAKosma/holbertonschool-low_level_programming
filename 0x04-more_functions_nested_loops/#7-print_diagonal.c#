#include "main.h"

/**
 * print_numbers - returns the upperase character
 * Return: uppercase character
 */

void print_line(int n)
{
	int i;
	int c;

	for (i = 0; i < n; i++)
	{
		for(c = 0; c < i; c++)
		{
			_putchar (32);
		}
       		_putchar (95);
	}
       	_putchar (10);
}
